# Cricket Team Data and Prediction Shiny App

This Shiny app allows users to explore cricket team data interactively and make predictions for team rankings using a Random Forest model.

## Getting Started

### Prerequisites

Make sure you have the following R packages installed:

- shiny
- ggplot2
- dplyr
- readxl
- randomForest
- plotly

You can install them using the following commands:

```R
install.packages(c("shiny", "ggplot2", "dplyr", "readxl", "randomForest", "plotly"))
